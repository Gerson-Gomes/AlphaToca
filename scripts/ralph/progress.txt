# Ralph Progress Log
Started: Thu May  7 04:25:36 PM -03 2026
---

## Codebase Patterns
- Migrations are hand-authored (`.env` points at live Supabase — never run `prisma migrate dev`). Verify byte-for-byte with `npx prisma migrate diff --from-empty --to-schema-datamodel prisma/schema.prisma --script`.
- New migration directory format: `prisma/migrations/YYYYMMDDHHMMSS_snake_case_name/migration.sql`. Prisma sorts alphabetically — use a strictly increasing timestamp.
- Service unit tests mock `../src/config/db` with `vi.mock()` at the top, then `import prisma` and cast methods to `ReturnType<typeof vi.fn>` (see `tests/rentalPaymentService.test.ts`).
- Services that use `logger` must also be mocked in tests — `vi.mock('../src/config/logger', () => ({ logger: { error: vi.fn(), warn: vi.fn(), info: vi.fn() } }))`.
- `req.localUser` is populated by `authSyncMiddleware` in `authStack = [checkJwt, authSyncMiddleware]`. On public routes (mounted without authStack — e.g. `GET /properties/:id`) it is undefined, so always guard with `req.localUser?.id ?? null`.
- Type augmentation for `req.localUser` lives in `src/types/express.d.ts`.
- Pre-existing test failures (21 tests in auth / ingestKnowledge / ragChainService / ragRetrieverService / userSync / visitValidation) are unrelated to LL-001 — confirmed by stash-and-rerun.
- HTTP tests that need to exercise BOTH the 401 stub AND the real 403 from `requireRole` MUST use `vi.mock('../src/middlewares/authMiddleware', async (importOriginal) => { const actual = await importOriginal() as any; return { ...actual, checkJwt: ..., authSyncMiddleware: ... }; })` — i.e. preserve `requireRole` from the real module. The plain object-literal mock form (see `conversationResolve.test.ts`) shadows `requireRole` with a pass-through and makes 403 untestable. See `tests/adminSupportTicketsList.test.ts` for the importOriginal pattern.
- Role-gated route mounts chain middlewares at the `app.use` level rather than spreading them per route: `app.use('/api', authStack, requireRole(Role.LANDLORD), landlordRoutes)`. Routes inside the router stay flat.
- `@db.Decimal(10, 2)` columns emit `DECIMAL(10,2)` (not `NUMERIC(10,2)`) in Prisma-generated DDL. When PRD acceptance criteria call for `NUMERIC`, follow `prisma migrate diff` output instead — byte-for-byte match is the ACTUAL acceptance gate. Deserialize Decimal → number in the view with `Number(row.field)` (matches `contractService.getActiveContractByPropertyAndTenant`).
- `prisma.$queryRaw` tagged-template substitutions (`${value}`) are parameterized as prepared-statement placeholders — safe from SQL injection. For UUID parameters on uuid-typed columns, add a `::uuid` cast in the SQL (`WHERE landlord_id = ${id}::uuid`) so Postgres accepts the string binding. `COUNT(*)` returns `bigint` from Prisma raw queries — coerce with `Number(row.count)` in the TS caller.
- In service unit tests that call `$queryRaw` multiple times, mock the method with `.mockResolvedValueOnce(...)` chains in the exact order the service issues queries. `vi.clearAllMocks()` in `beforeEach` resets the chain between tests.
- Per-route role gating on `propertyRoutes` (mounted without authStack at `app.use('/api', propertyRoutes)`) uses a local `landlordAuthStack = [checkJwt, authSyncMiddleware, requireRole(Role.LANDLORD)]` spread on the route, mirroring the existing `adminAuthStack` pattern. Don't move `propertyRoutes` under the global `authStack` mount — too many routes in that file are intentionally public (getById, search, list).
- Route-specific paths MUST be registered before the `:id` param route in the router file. Express path-matching rejects `/properties/:id` for `/properties/analytics/monthly` by segment count (3 vs 2), BUT keeping specific routes above `:id` avoids surprises when anyone later adds a `:id/subroute`. Place new specific GETs right after `/properties/search`.
- HTTP tests for controllers that delegate to a service module: mock the service, not `prisma`. Example for LL-005: `vi.mock('../src/services/analyticsService', () => ({ analyticsService: { monthlySeries: mockMonthlySeries } }))`. Then assert the service was called with the controller-parsed args (e.g. Date objects, landlordId from req.localUser).
- Fire-and-forget tracking services (profileView/propertyView) all follow the same shape: try/catch wrapping a conditional dedup findFirst + create, with errors swallowed via `logger.error`. When a tracking call also mutates a sibling table (e.g. `Property.views` counter in LL-006), keep BOTH writes inside the same try block so a failure in either surfaces only in logs — the controller stays unaware.
- NOT every analytics event service is fire-and-forget. Contact-click (LL-007) is awaited and propagates errors — the HTTP endpoint `POST /:id/contact-click` is called synchronously from the UI and the caller expects a 201, so a DB failure should surface as 500, not a silently-logged no-op. Rule of thumb: service is fire-and-forget when wrapping a read (GET/details); service is awaited when the client explicitly called an event-recording endpoint.
- PUBLIC routes on `propertyRoutes` (mounted at `app.use('/api', propertyRoutes)` without any global auth) can still read `req.localUser?.id ?? null` — if a future middleware populates localUser optionally, the service gets it; otherwise null. Test the authenticated branch by building a mini express app that injects `req.localUser` via a tiny middleware then mounts the production router. See `tests/contactClickEvent.test.ts::buildAuthedApp`.
- When co-locating HTTP tests (vi.mock at top level for the service) AND direct service unit tests in the same file, `vi.doMock` inside a nested describe cannot override the top-level service mock because the module graph is already resolved. Solution: split service unit tests into a sibling `*Service.test.ts` file. Avoid trying to toggle mocks per-describe via `resetModules` — the cached imports will bite you.
- For per-property ownership-gated reads (e.g. LL-008 analytics), the guard ordering is: 401 (no `req.localUser`) → 400 (Zod params/query) → 404 (missing property, BEFORE the ownership check) → 403 (exists but caller !== landlordId). The 404-before-403 order prevents an existence oracle — swapping it lets callers probe "is this uuid a real property I don't own?" via the response code. Test BOTH the owner-404 AND the intruder-404 branches.
- Aggregating multiple independent Prisma counts in a service? Fire them in `Promise.all([...])` — the Prisma driver supports concurrent statements outside a transaction, and the total latency collapses from sum-of-queries to max-of-queries. Use this shape when the counts share no state (LL-008 did 6 counts + 1 raw group-by in parallel). Do NOT wrap them in `$transaction([...])` unless you actually need a consistent snapshot — transactions serialize.
- For daily-granularity analytics buckets, use `to_char(ts AT TIME ZONE 'UTC', 'YYYY-MM-DD')` in the `$queryRaw` group-by — the explicit `AT TIME ZONE 'UTC'` avoids drift when Postgres's session timezone differs from UTC. Zero-fill in TS via an `enumerateDays(fromInclusive, toExclusive)` walker + `Map.get(date) ?? 0` lookup, same pattern as `enumerateMonths` in `analyticsService.ts`.
- Ownership-gated route tests can run with a single app instance if the authMiddleware mock uses a header-driven switch (`Bearer landlord-owner` vs `Bearer landlord-intruder`) that remaps `req.localUser.id` to different UUIDs. This is the `rentalPaymentCurrent.test.ts` pattern — reuse it for any new owner-only route so you exercise the 200, 403, and 404 branches from the same `describe`.

## 2026-05-07 - LL-001 — Profile view tracking table + increment hook
- Added `ProfileView` Prisma model (landlordId, viewerId?, viewedAt) + inverse relation on `User` + `@@index([landlordId, viewedAt])` + onDelete Cascade.
- Hand-authored `prisma/migrations/20260507200000_add_profile_view/migration.sql` — verified byte-for-byte against `prisma migrate diff --from-empty`.
- Created `src/services/profileViewService.ts::record(landlordId, viewerId?)` — fire-and-forget, swallows DB errors via `logger.error`, 24h dedup via `findFirst { viewedAt: { gte: now-24h } }` when `viewerId` non-null, anonymous viewers always insert.
- Wired into `propertyController.getById` with `if (req.query.inspectLandlord === 'true') { void profileViewService.record(property.landlordId, req.localUser?.id ?? null) }` AFTER the 404 guard, non-blocking.
- Files changed: `prisma/schema.prisma`, `prisma/migrations/20260507200000_add_profile_view/migration.sql`, `src/services/profileViewService.ts`, `src/controllers/propertyController.ts`, `tests/profileViewService.test.ts`.
- Tests: 6/6 new service tests pass (authenticated insert, 24h dedup skip, anonymous always insert, default viewerId=null, DB error swallowed, 24h window math).
- `npx tsc --noEmit` clean.
- **Learnings for future iterations:**
  - `GET /api/properties/:id` is mounted WITHOUT authStack (`app.use('/api', propertyRoutes)` at src/app.ts:95, not inside `authStack`). So `req.localUser` is undefined for anonymous callers — the `?? null` fallback is load-bearing.
  - Grepped for `inspectLandlord` and `/api/landlord/:id` — neither exists in this repo yet. Went with the query-param hook as primary per PRD AC. LL-002 will add `/api/landlord/metrics`; a dedicated landlord profile route is not in scope.
  - `npx prisma migrate diff --from-migrations` fails without `--shadow-database-url`. Use `--from-empty --to-schema-datamodel ... --script` for byte-for-byte DDL verification — that's what matches the PRD AC wording.
  - Existing migration timestamps ended at `20260507190000`. Next migrations should use strictly later timestamps to preserve lex order.
  - Full suite currently has 21 pre-existing failing tests unrelated to this story (confirmed by `git stash` + rerun on the same test file). Do not treat these as regressions; future stories can ignore them unless explicitly touching those modules.
---

## 2026-05-07 - LL-002 — GET /api/landlord/metrics
- Added `src/controllers/landlordController.ts::getMetrics` returning `{ profileViews, proposalsPending, unreadMessages }` in a single round trip.
  - profileViews: `prisma.profileView.count` where `landlordId = req.localUser.id` AND `viewedAt >= now()-30d`.
  - proposalsPending: `prisma.proposal.count` where `status=PENDING` AND `property.landlordId = req.localUser.id` (relation filter — no need to join manually).
  - unreadMessages: `prisma.conversationMessage.count` where `readAt=null` AND `authorId != landlordId` AND `conversation.landlordId = landlordId`. Wrapped in try/catch that maps Prisma P2021 ("table does not exist") → 0 so the endpoint is safe to deploy BEFORE LL-010 ships.
- Added `src/routes/landlordRoutes.ts` with a full Swagger JSDoc block and `router.get('/landlord/metrics', landlordController.getMetrics)` — auth middlewares live at the mount site, not the route site, matching the conversationRoutes template.
- Mounted in `src/app.ts` as `app.use('/api', authStack, requireRole(Role.LANDLORD), landlordRoutes)` — TENANT and ADMIN both get 403 from the real requireRole.
- Files changed: `src/controllers/landlordController.ts` (new), `src/routes/landlordRoutes.ts` (new), `src/app.ts` (import + mount), `tests/landlordMetrics.test.ts` (new).
- Tests: 6/6 new tests pass — happy path with exact shape assertions + 30d window math, P2021 graceful fallback, zero-activity landlord, 401 (no header), 403 tenant, 403 admin.
- `npx tsc --noEmit` clean.
- Ran `conversationResolve + adminSupportTicketsList + rentalPaymentCurrent + profileViewService` alongside — 44/44 pass, no regressions in adjacent HTTP/auth code.
- **Learnings for future iterations:**
  - Prisma `relation filter` syntax (`where: { property: { landlordId } }`) avoids a manual join for "count Proposal where its Property belongs to landlord X" — cleaner than `$queryRaw`, still pushed into one SQL statement.
  - Casting `prisma as any` at the call site of a not-yet-generated model (`(prisma as any).conversationMessage.count(...)`) lets the endpoint compile and exercise the P2021 fallback today, without waiting for LL-010's `prisma generate`. When LL-010 lands, drop the `as any`.
  - Testing 403 from `requireRole` requires the `importOriginal` mock variant — pattern now captured in the Codebase Patterns section above.
  - Mocking `../src/config/db` as a default-export object (`{ default: { profileView: { count: mockFn }, ... } }`) is the right shape for controllers that do `import prisma from '../config/db'`. Namespace-level model functions like `.count` can be driven per-test with `mockFn.mockResolvedValue(...)`.
---

## 2026-05-07 - LL-003 — Add RentalPayment.amount column + backfill migration
- Added nullable `amount Decimal? @db.Decimal(10, 2)` to the `RentalPayment` model in `prisma/schema.prisma`.
- Hand-authored `prisma/migrations/20260507210000_add_rental_payment_amount/migration.sql` — single `ALTER TABLE "rental_payments" ADD COLUMN "amount" DECIMAL(10,2);` + correlated UPDATE backfill pulling `contracts.monthly_rent` for each property's ACTIVE contract (LIMIT 1 per propertyId). Header comment matches PRD wording verbatim.
- Verified byte-for-byte: `npx prisma migrate diff --from-empty --to-schema-datamodel prisma/schema.prisma --script` emits `"amount" DECIMAL(10,2),` inside the rental_payments CREATE TABLE — the ADD COLUMN in my migration matches that column declaration.
- `npx prisma validate` + `npx prisma generate` both succeed.
- Updated `src/services/rentalPaymentService.ts`:
  - `RentalPaymentView` now carries `amount: number | null`.
  - `getCurrent` selects `amount` and serializes Decimal → number via `Number(row.amount)`; default-shape (no row) returns `amount: null`.
  - `upsertCurrent` calls a new `getActiveMonthlyRent(propertyId)` helper that queries `prisma.contract.findFirst({where: {propertyId, status: 'ACTIVE'}, orderBy: {createdAt: 'desc'}})` and returns `Number(monthlyRent)` or `null`. The snapshot goes into BOTH `create.amount` and `update.amount` so subsequent writes re-read the then-active rent (matches PRD §8 Q2 "no historical preservation").
- Swagger JSDoc for `GET|PUT /properties/:id/payments/current` in `src/routes/propertyRoutes.ts` now documents the `amount` field (nullable float in BRL, snapshot-at-write semantics).
- Files changed: `prisma/schema.prisma`, `prisma/migrations/20260507210000_add_rental_payment_amount/migration.sql`, `src/services/rentalPaymentService.ts`, `src/routes/propertyRoutes.ts`, `tests/rentalPaymentService.test.ts` (updated to match new select/shape + mock `prisma.contract.findFirst`), `tests/rentalPaymentAmount.test.ts` (new — 5 focused tests).
- Tests: 25/25 pass across `rentalPaymentAmount.test.ts` (5) + `rentalPaymentService.test.ts` (10) + `rentalPaymentCurrent.test.ts` (10). The HTTP controller tests in `rentalPaymentCurrent.test.ts` mock the service layer, so they continue to pass unchanged — the amount field flows through the controller as-is.
- `npx tsc --noEmit` clean. Full suite: 422/443 pass — the 21 failures are the same pre-existing set captured in the Codebase Patterns section (auth/userSync/ingestKnowledge/rag*/visitValidation), unchanged by this story.
- **Learnings for future iterations:**
  - Prisma Decimal columns round-trip as Decimal objects (not native number). In this codebase, views cast with `Number(row.field)` — consistent with `contractService.getActiveContractByPropertyAndTenant` (src/services/contractService.ts:249). `Number()` is null-safe only if you handle the null case separately (my code uses `row.amount === null ? null : Number(row.amount)`).
  - `prisma migrate diff --from-empty --to-schema-datamodel` emits `DECIMAL(10,2)` for `@db.Decimal(10, 2)` fields. PRD drafts that specify `NUMERIC(10,2)` should be read as semantic intent — the byte-for-byte acceptance gate follows Prisma's output. NUMERIC and DECIMAL are synonyms in Postgres, but the migration string must match the diff.
  - `upsertCurrent` now does one extra read (active-contract lookup) before the upsert. Acceptable because GET/PUT payments/current is one-property-at-a-time; the lookup is indexed on `contracts.property_id`. If future analytics endpoints SUM(amount) across many properties, they read the already-persisted snapshot — no N+1 on that path.
  - When updating a service signature, the downstream HTTP test (rentalPaymentCurrent.test.ts) that mocks `rentalPaymentService.upsertCurrent` entirely keeps passing because the mock supplies the full response shape. But the *service-level* test (rentalPaymentService.test.ts) needed to be updated to mock the new `prisma.contract.findFirst` — without that mock, `upsertCurrent` throws `findFirst is not a function` during the active-rent lookup.
  - Next migration timestamp should be `>= 20260507210001` to preserve strict lex order. Existing ceiling is now `20260507210000`.
---

## 2026-05-07 - LL-004 — analyticsService.monthlySeries helper
- Added `src/services/analyticsService.ts` exposing `monthlySeries(landlordId, from, to): Promise<MonthlySeries>`.
  - `months`: inclusive YYYY-MM list produced by an in-process `enumerateMonths(from, to)` walker (UTC-aligned with `currentPeriod` in rentalPaymentService) — handles year-boundary crossings, single-month ranges, and `from > to` (returns empty arrays, zero queries issued).
  - `rentals[m]`: `$queryRaw` over `contracts` grouped by `to_char(start_date, 'YYYY-MM')`, filtered by `landlord_id = ${landlordId}::uuid AND start_date >= ${rangeStart} AND start_date < ${rangeEndExclusive}`.
  - `newTenants[m]`: subquery `SELECT tenant_id, MIN(start_date) AS first_start FROM contracts WHERE landlord_id = ${landlordId}::uuid GROUP BY tenant_id` wrapped in an outer `WHERE first_start >= rangeStart AND first_start < rangeEndExclusive GROUP BY to_char(first_start, 'YYYY-MM')`. Matches PRD AC: only the tenant's FIRST contract with this landlord counts.
  - `monthlyRevenue[m]`: joins `rental_payments` ↔ `properties`, filters by `p.landlord_id = ${landlordId}::uuid AND rp.status = 'PAID' AND rp.period BETWEEN fromPeriod AND toPeriod`, `COALESCE(SUM(rp.amount), 0) AS revenue`. Revenue bucket is 0 for months with no paid rows even when Postgres never emits a row for that period (zero-fill via `Map.get(m) ?? 0` in TS).
- Arrays are guaranteed parallel to `months` via a Map lookup + default for every entry.
- All three queries use `$queryRaw` tagged-template substitutions — `landlordId` is a prepared-statement parameter, NOT string-interpolated (PRD AC explicit).
- Files changed: `src/services/analyticsService.ts` (new), `tests/analyticsService.test.ts` (new — 7 tests).
- Tests: 7/7 pass — 3-month happy path with exact values, gap-month zero-fill, fully empty range, year-boundary (Nov 2025 → Feb 2026 = 4 months), single-month range, from > to early-return, null COALESCE → 0 coercion.
- `npx tsc --noEmit` clean. Full suite: 429/450 pass — the 21 failures are the same pre-existing set documented in the Codebase Patterns section; net delta is +7 from the new test file.
- **Learnings for future iterations:**
  - Prisma `$queryRaw` returns `bigint` for `COUNT(*)` — coerce with `Number(row.count)` in the TS layer. Tests can feed `2n` (BigInt literal) to the mock to exercise the cast.
  - `to_char(ts, 'YYYY-MM')` in Postgres emits zero-padded months without extra formatting. Aligning ranges on `Date.UTC(year, month, 1)` keeps the JS side in sync with the server-computed period (same choice as `currentPeriod` in rentalPaymentService — no fuso off-by-one).
  - For UUID columns (like `contracts.landlord_id`), an explicit `${id}::uuid` cast is required when binding a JS string via `$queryRaw`. Without it Postgres raises `invalid input syntax for type uuid` — this repo's `propertyService.ts` established the `::uuid` pattern.
  - `$queryRaw` is mockable with `.mockResolvedValueOnce(...)` chained in strict call order. The service's call sequence (rentals → newTenants → revenue) is now a load-bearing contract that tests encode — if you re-order the calls in the service, update the test queues to match.
  - `analyticsService` is ready for LL-005 to consume — the HTTP handler will just validate the range, call `analyticsService.monthlySeries(req.localUser.id, from, to)`, and return the result shape as-is.
  - Next migration timestamp should be `>= 20260507210001` (unchanged — this story added no migrations).
---

## 2026-05-07 - LL-005 — GET /api/properties/analytics/monthly
- Added `src/utils/analyticsValidation.ts::monthlyAnalyticsQuerySchema` — Zod validates optional `from`/`to` against `^\d{4}-(0[1-9]|1[0-2])-01$`, refines that `from ≤ to`, and rejects inclusive month-span > 24. When either bound is absent, the controller falls back to the last-6-months default (no partial custom ranges).
- Added `propertyController.getMonthlyAnalytics` — parses the query, computes UTC defaults (`to = first day of current UTC month`, `from = 5 months earlier`), delegates to `analyticsService.monthlySeries(localUser.id, from, to)`, and returns the series as-is.
- Wired `router.get('/properties/analytics/monthly', ...landlordAuthStack, propertyController.getMonthlyAnalytics)` in `src/routes/propertyRoutes.ts`, spreading a new `landlordAuthStack = [checkJwt, authSyncMiddleware, requireRole(Role.LANDLORD)]` (follows the existing `adminAuthStack` pattern). Route placed immediately after `/properties/search`, well above `/properties/:id`.
- Added a full Swagger JSDoc block documenting both default + custom ranges, the 24-month ceiling, the parallel-array response shape, and 400/401/403 contracts.
- Files changed: `src/utils/analyticsValidation.ts` (new), `src/controllers/propertyController.ts` (imports + new `getMonthlyAnalytics`), `src/routes/propertyRoutes.ts` (Role import, landlordAuthStack, new route + Swagger), `tests/propertiesAnalyticsMonthly.test.ts` (new — 9 tests).
- Tests: 9/9 pass — default range asserts exact `from`/`to` Dates sent to `monthlySeries`, custom range asserts ISO parity, 400 oversized-span, 400 `from > to`, 400 malformed (non-first-of-month), 200 at-boundary exactly-24 months span, 401 no-header, 403 tenant, 403 admin.
- `npx tsc --noEmit` clean. Full suite: 438/459 pass — the 21 failures are exactly the same pre-existing set (auth/userSync/ingestKnowledge/rag*/visitValidation); net delta is +9 new passing tests, zero regressions.
- **Learnings for future iterations:**
  - `propertyRoutes` is mounted without any global authStack (see `src/app.ts:97`), so every protected route in that file spreads auth middleware inline. The local `adminAuthStack` / new `landlordAuthStack` / `authStack` arrays are the established pattern — don't wrap the whole router in a single middleware.
  - The last-6-months default uses the same UTC-month calculus as `rentalPaymentService.currentPeriod`: `Date.UTC(year, month, 1)` for the "end" bound and `month - 5` for the start. `Date.UTC` normalizes the month arithmetic on its own (e.g. `month = -1` → December of previous year), no manual wrap-around needed. In tests, derive expected Dates from `new Date()` inside the test body rather than hardcoding — otherwise the test will rot the first time it runs in a different calendar month.
  - When a controller delegates to a service, mock the service layer in the HTTP test (not `prisma`). It isolates behavior to the handler contract (parsed args in → response out), and keeps the test robust to service-internal refactors like LL-004's three-query-raw chain.
  - Zod's `superRefine` runs AFTER individual field regex checks — so when both bounds are malformed, the refine body is skipped entirely (both fields already yielded issues). This is why the "from > to" test uses valid ISO strings: the refine branch only runs when both pass the regex.
  - Next migration timestamp ceiling is still `>= 20260507210001` (this story added no migrations; LL-006 onward will need fresh timestamps).
---

## 2026-05-07 - LL-006 — PropertyViewEvent table + increment hook
- Added `PropertyViewEvent` Prisma model (`propertyId`, `viewerId?`, `viewedAt`) + inverse relation `viewEvents` on `Property` + `@@index([propertyId, viewedAt])` + `onDelete: Cascade` on property FK.
- Hand-authored `prisma/migrations/20260507220000_add_property_view_event/migration.sql` — verified byte-for-byte against `prisma migrate diff --from-empty --to-schema-datamodel prisma/schema.prisma --script` (CREATE TABLE + CREATE INDEX + FK all match exactly).
- `npx prisma validate` + `npx prisma generate` both succeed.
- Created `src/services/propertyViewService.ts::record(propertyId, viewerId?)` — fire-and-forget:
  - 1h dedup for authenticated viewers via `findFirst { propertyId, viewerId, viewedAt: { gte: now-1h } }`. Anonymous (`viewerId=null`) always inserts.
  - On insert, ALSO increments the pre-existing `Property.views` aggregate counter via `prisma.property.update({ data: { views: { increment: 1 } } })` — preserves FR-12 so `orderBy=views` in the search endpoint still works. Deduped requests do NOT double-count the counter.
  - Errors in either write swallowed via `logger.error` so tracking failures never surface to callers.
- Wired into `propertyController.getById` with `void propertyViewService.record(property.id, req.localUser?.id ?? null)` — placed AFTER the 404 guard, AFTER the LL-001 profileView hook, ALWAYS runs (no query-param gate — every property detail open counts). Non-blocking.
- Files changed: `prisma/schema.prisma`, `prisma/migrations/20260507220000_add_property_view_event/migration.sql`, `src/services/propertyViewService.ts` (new), `src/controllers/propertyController.ts` (import + hook), `tests/propertyViewService.test.ts` (new).
- Tests: 7/7 new service tests pass — authenticated insert + counter increment, 1h dedup skip (no counter bump), anonymous always inserts + increments, default viewerId=null, DB error swallowed (create throws), DB error swallowed (property.update throws), 1h window math.
- `npx tsc --noEmit` clean. Full suite: 445/466 pass — the 21 failures are exactly the same pre-existing set (auth/userSync/ingestKnowledge/rag*/visitValidation); net delta is +7 new passing, zero regressions.
- **Learnings for future iterations:**
  - `Property.views` has existed as a schema field for ages but nothing in the repo was actually incrementing it prior to this story (grepped `views: { increment` and `views\s*:\s*\{\s*increment` — zero hits). The AC wording "still incremented" read aspirationally — this story is effectively wiring the counter for the first time. The service doing it preserves the `orderBy=views` search path instead of deprecating it.
  - When a property detail read both triggers a profileView (conditional on `?inspectLandlord=true`) and a propertyView (unconditional), the controller should call both fire-and-forget in parallel. Keep them as separate `void service.record(...)` calls rather than collapsing into one — each has different dedup windows (24h vs 1h) and different target tables.
  - The 1h dedup window (vs 24h for ProfileView) is intentional: daily-granularity analytics should treat "returned to the listing after an hour" as a new data point; a single F5 on the same minute shouldn't. Shorter window = more events per day per user = denser daily series.
  - Test mock shape for `prisma as default export` with nested models needs BOTH paths when the service touches two models — `{ default: { propertyViewEvent: {...}, property: {...} } }`. Missing the `property.update` mock silently swallows the error via try/catch and the test passes for the wrong reason (no TypeError). Always include a `mockPropertyUpdate.mockResolvedValue(...)` alongside `mockCreate.mockResolvedValue(...)` in positive-path tests.
  - Next migration timestamp ceiling is now `>= 20260507220001`. LL-007 needs a fresh stamp (add_contact_click_event).
---

## 2026-05-07 - LL-007 — ContactClickEvent table + POST /api/properties/:id/contact-click
- Added `ContactClickEvent` Prisma model (`propertyId`, `viewerId?`, `clickedAt`) + inverse relation `contactClickEvents` on `Property` + `@@index([propertyId, clickedAt])` + `onDelete: Cascade` on property FK.
- Hand-authored `prisma/migrations/20260507230000_add_contact_click_event/migration.sql` — verified byte-for-byte against `prisma migrate diff --from-empty --to-schema-datamodel prisma/schema.prisma --script` (CREATE TABLE + CREATE INDEX + FK all match exactly).
- `npx prisma validate` + `npx prisma generate` both succeed.
- Created `src/services/contactClickEventService.ts::record(propertyId, viewerId?)` — awaited (NOT fire-and-forget). Just inserts a row via `prisma.contactClickEvent.create`, with NO dedup (every click is a real analytics signal, even from the same user). Errors are logged AND propagated so the controller can map to HTTP 500.
- Added `propertyController.recordContactClick` — Zod-validates path `:id` as a UUID (400 otherwise), looks up `propertyService.getPropertyById` (404 if missing), then awaits `contactClickEventService.record(id, req.localUser?.id ?? null)` and returns 201 `{}`. The `?? null` fallback is load-bearing because the route is PUBLIC (no authStack).
- Registered `router.post('/properties/:id/contact-click', propertyController.recordContactClick)` in `src/routes/propertyRoutes.ts` — placed BEFORE the `/properties/:id` GET to respect the "specific-route-before-param-route" Codebase Pattern. Added full Swagger JSDoc documenting PUBLIC semantics, no-dedup, 201/400/404 contracts.
- Files changed: `prisma/schema.prisma`, `prisma/migrations/20260507230000_add_contact_click_event/migration.sql` (new), `src/services/contactClickEventService.ts` (new), `src/controllers/propertyController.ts` (imports + new `recordContactClick` + Zod params schema), `src/routes/propertyRoutes.ts` (Swagger JSDoc + route registration), `tests/contactClickEvent.test.ts` (new — 5 HTTP tests), `tests/contactClickEventService.test.ts` (new — 5 service tests).
- Tests: 10/10 new pass — anonymous click (201, viewerId=null), authenticated click (201, viewerId forwarded via mini-app with pre-injecting middleware), 404 unknown property, 400 malformed UUID, PUBLIC route assertion (no 401 without auth header); service-level: authed + anon + default viewerId + no-dedup on repeat calls + error propagation.
- `npx tsc --noEmit` clean. Full suite: 455/476 pass — the 21 failures are exactly the same pre-existing set (auth/userSync/ingestKnowledge/rag*/visitValidation); net delta is +10 new passing, zero regressions.
- **Learnings for future iterations:**
  - Contact-click is the first analytics-event service in this epic that is AWAITED rather than fire-and-forget. The semantic difference is captured in the Codebase Patterns section: tracking-on-read (profileView/propertyView) is fire-and-forget; tracking-on-explicit-event (contactClick) is awaited so 500s surface. Future event endpoints should follow the same rule.
  - For PUBLIC routes that still want to forward an optional `req.localUser.id` to the service, the fallback `req.localUser?.id ?? null` is enough — no need to add an "optional auth" middleware. Tests should exercise BOTH branches: production HTTP (anonymous, localUser undefined) AND a mini-app that injects localUser before mounting the real router.
  - Co-locating HTTP tests (vi.mock at module top) with direct service unit tests in the same file does NOT work — vi.doMock inside a nested describe can't override the top-level service mock because the module graph is already resolved at import time. Solution: split into two files (`contactClickEvent.test.ts` for HTTP, `contactClickEventService.test.ts` for service). Discovered the hard way when 3 service tests failed with "vi.fn() not called" despite the service being correct.
  - The `vi.hoisted` pattern for shared mock handles works well when the mocks need to be referenced both in `vi.mock` factories AND inside test assertions. All mock factory code runs before imports, so `vi.fn()` declared via `vi.hoisted` is stable across hoisting.
  - Next migration timestamp ceiling is now `>= 20260507230001`. LL-010 (ConversationMessage) is the next migration-writing story.
---

## 2026-05-07 - LL-008 — GET /api/properties/:id/analytics
- Added `src/services/propertyAnalyticsService.ts::getAnalytics(propertyId, window)` — aggregates 6 counts + 1 `$queryRaw` daily group-by in a single `Promise.all([...])`. Window math is UTC: `toExclusive = startOfUtcDay(now) + 1d`, `fromInclusive = toExclusive - WINDOW_DAYS[window]` (30 / 90 / 365).
  - views: `prisma.propertyViewEvent.count` in window.
  - favorites: `prisma.favorite.count` all-time.
  - proposalsTotal: `prisma.proposal.count` filtered by `createdAt` in window.
  - proposalsOpen: `prisma.proposal.count` all-time with `status=PENDING` (uses `ProposalStatus.PENDING` enum, not string literal).
  - visitsScheduled: `prisma.visit.count` all-time with `status=SCHEDULED` (`VisitStatus.SCHEDULED`).
  - contactClicks: `prisma.contactClickEvent.count` in window (filtered by `clickedAt`).
  - dailyViews: `$queryRaw` with `to_char(viewed_at AT TIME ZONE 'UTC', 'YYYY-MM-DD') AS bucket, COUNT(*)` grouped by bucket, zero-filled via an `enumerateDays(fromInclusive, toExclusive)` walker + `Map.get(date) ?? 0`.
- Added `propertyAnalyticsQuerySchema` (window enum: `30d | 90d | 1y`, optional) and `propertyAnalyticsParamsSchema` (uuid) to `src/utils/analyticsValidation.ts`.
- Added `propertyController.getPropertyAnalytics` — guard order: 401 (no localUser) → 400 (Zod path uuid + query window enum) → 404 (missing property, BEFORE the ownership check) → 403 (exists but caller !== landlordId). Default window is `'30d'` when the query param is omitted. Delegates to the new service.
- Registered `router.get('/properties/:id/analytics', ...authStack, propertyController.getPropertyAnalytics)` in `src/routes/propertyRoutes.ts`, placed BEFORE the `/properties/:id` GET so the Express path-matching resolves the longer path first (and because of the "specific-route-before-param-route" Codebase Pattern). Uses `authStack` (not `landlordAuthStack`) — the role gate is already implicit via the per-property ownership check, so TENANT → 404/403 depending on whether the property exists, never a blanket 403.
- Added a full Swagger JSDoc block documenting the window enum, each counter's semantics, the dailyViews shape, and the 400/401/403/404 contracts.
- Files changed: `src/services/propertyAnalyticsService.ts` (new), `src/utils/analyticsValidation.ts` (add 2 schemas), `src/controllers/propertyController.ts` (imports + new `getPropertyAnalytics`), `src/routes/propertyRoutes.ts` (Swagger JSDoc + route registration), `tests/propertyAnalytics.test.ts` (new — 9 HTTP tests), `tests/propertyAnalyticsService.test.ts` (new — 6 service tests).
- Tests: 15/15 new pass — HTTP: default-window=30d, all 3 window values forwarded, dailyViews shape preserved, 400 malformed uuid, 400 bad window enum, 401 no header, 404 on missing property (both owner and intruder — no existence leak), 403 on non-owner of existing property; service: shape with frozen clock, full zero-fill for empty window, 90d series has 90 buckets, 1y series has 365 buckets, filter-scope assertion for each count (propertyId / status / date), bigint → number coercion in dailyViews.
- `npx tsc --noEmit` clean. Full suite: 470/491 pass — the 21 failures are exactly the same pre-existing set (auth/userSync/ingestKnowledge/rag*/visitValidation); net delta is +15 new passing, zero regressions.
- **Learnings for future iterations:**
  - `Promise.all([...])` across 6 independent `prisma.<model>.count` calls + 1 `$queryRaw` was intentional — no shared state, no transaction needed. Collapses the endpoint latency to the slowest single query. Do NOT wrap in `$transaction([...])`: that serializes and defeats the parallelism.
  - The 404-before-403 ordering is load-bearing: if you check ownership first, 403 on a missing id would leak "this uuid IS a real property, you just don't own it." Tested both branches (owner-404 + intruder-404) to lock the ordering in.
  - Used `authStack` (not `landlordAuthStack`) for this route because the ownership check already filters non-landlords (they'd always get 403 on someone else's property, and landlords-without-this-property also get 403). Adding `requireRole(LANDLORD)` would short-circuit TENANT callers to 403 before the 404 path — arguably fine, but the PRD wording "inline ownership check" suggested deferring to the per-property guard. Net effect for TENANT: 404 on missing property, 403 on existing property they don't own (same info as LANDLORD non-owner).
  - `vi.useFakeTimers() + vi.setSystemTime(FIXED_NOW)` inside the service unit test freezes `new Date()` so the window math is deterministic across runs — without it, the "last bucket should be today" assertion rots daily. Remember to `vi.useRealTimers()` in `afterEach` or other tests using timers misbehave.
  - The `AT TIME ZONE 'UTC'` cast in the `to_char` expression is defensive: Postgres's session timezone defaults to whatever the connection negotiates, and Supabase sets it to UTC, but a future rebind to a different pool could introduce a silent off-by-one on the daily bucket. Explicit UTC matches the in-process `enumerateDays` walker which uses `Date.UTC`.
  - No new migrations in this story — timestamp ceiling remains `>= 20260507230001` for LL-010 onward.
---
