# Ralph Progress Log
Started: Wed May 13 04:20:36 PM -03 2026
---

## Codebase Patterns
- `src/app.ts` mounts `express.static` BEFORE the `authStack` so static assets bypass `checkJwt`. Any new public file route must follow the same ordering.
- Static-file responses are customized via `express.static(root, { setHeaders })` — the callback signature is `(res: ServerResponse) => void`, NOT `(res: Response) => void`. Import `ServerResponse` from `node:http` (`import type { ServerResponse } from 'http'`) for the type.
- Image upload URLs are stored relative as `/uploads/<propertyId>/<file>.ext` (see `propertyImageStorageService.savePropertyImages`). Contract PDFs follow the same shape under `/uploads/contract-<id>/<file>.pdf` (see `contractDocumentStorageService.saveContractDocument`). DO NOT migrate these to absolute URLs — the backend dual-mount in `/uploads` and `/api/uploads` is the supported way to make them resolve.
- Tests in `tests/` follow a `supertest` + `request(app)` pattern. `tests/contractPdf.test.ts` is the canonical reference for tests that need a real fixture file under `uploads/` — it uses `beforeAll`/`afterAll` to mkdir/rm a temp dir under `UPLOADS_ROOT = path.resolve(__dirname, '../uploads')`.
- Tests that import `src/app` must set Firebase env vars (FIREBASE_PROJECT_ID, FIREBASE_CLIENT_EMAIL, FIREBASE_PRIVATE_KEY) BEFORE the `import app from '../src/app'` line, because `validateAuthConfig()` runs at module load.
- `npm run build` runs `tsc` (no emit issues at HEAD). Use it as the typecheck gate before commit.

---

## 2026-05-13 - US-001
- Added dual `express.static` mount in `src/app.ts`: `/uploads` (new — matches what Flutter clients build via baseUrl + image.url) and `/api/uploads` (legacy compat). Both share a single `setUploadsHeaders` callback that emits `Cross-Origin-Resource-Policy: cross-origin` and `Cache-Control: public, max-age=86400`.
- Inline comment explains the rationale and references `tasks/prd-fix-image-serving-mismatch.md` so the dual mount is not deleted in a future refactor.
- Both mounts sit at lines 66-67, ahead of every `app.use('/api', authStack, ...)` line, so static files never hit `checkJwt`.
- Files changed: `src/app.ts` (added `ServerResponse` type import, hoisted `uploadsRoot` const, added `setUploadsHeaders` helper, added `/api/uploads` mount).
- `npm run build` exits 0. `grep -n "express.static" src/app.ts` returns 2 lines (66, 67) as required by the acceptance criteria.
- **Learnings for future iterations:**
  - PRD text described the source mount as `/api/uploads`, but `git log` on `src/app.ts` shows it had been hotfix-reverted to `/uploads` only. End state — both prefixes mounted — matches PRD intent regardless of starting position. Future iterations: always cross-check `git log -p -- <file>` against PRD assumptions.
  - The branch `main` had drifted 30 commits ahead of the local checkout. The Ralph-managed files (`scripts/ralph/{.last-branch,prd.json,progress.txt}`) had three-way merge conflicts on pull. Resolution: `git checkout --theirs` on those files preserved the new Ralph cycle's state; the previous cycle's artifacts had already been archived to `scripts/ralph/archive/2026-05-13-supabase-to-localhost-db-migration/`.
  - `node_modules` is NOT checked in and was missing on fresh checkout — `npm install` is a required prerequisite before `npm run build`.
  - `express.static`'s `setHeaders` callback receives a `http.ServerResponse`, not an Express `Response`. Use the node `http` type to keep the typecheck strict and avoid `any`.
---
